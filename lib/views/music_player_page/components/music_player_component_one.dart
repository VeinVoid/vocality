import 'package:flutter/material.dart';

class MusicPlayerComponentOne extends StatelessWidget {
  const MusicPlayerComponentOne({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Row()
      ],
    );
  }
}